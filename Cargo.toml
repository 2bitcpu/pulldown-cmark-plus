[package]
name = "pulldown-cmark-plus"
version = "0.1.4"
edition = "2024"

description = "pulldown-cmark custom library"
license = "MIT"

[dependencies]
pulldown-cmark = "0.13.0"

[profile.release]
opt-level = "s"
lto = true
strip = true
codegen-units = 1
panic = "abort"
